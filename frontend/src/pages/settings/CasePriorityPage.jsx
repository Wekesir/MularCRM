import { Flag } from 'lucide-react';
import PlaceholderModulePage from '../../components/PlaceholderModulePage';

function CasePriorityPage() {
  return (
    <PlaceholderModulePage
      icon={Flag}
      title="Case Priority"
      description="Configure the priority levels used to triage cases."
    />
  );
}

export default CasePriorityPage;
